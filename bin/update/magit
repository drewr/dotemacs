#!/bin/sh

SRC=$HOME/src/magit
DEST=d/lisp

die() {
  echo $1
  exit 1
}

[ -d $SRC ] || die "$SRC not found"
[ -d $DEST ] || mkdir -p $DEST
( cd $SRC && tar cf - magit.el ) \
   | ( cd $DEST && tar xvf - )
