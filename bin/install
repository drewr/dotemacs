#!/bin/sh

DEST=$HOME/.emacs.d

( cd d; tar cf - . ) \
| ( mkdir -p $DEST; \
    cd $DEST; \
    tar xvf - )

( cd $HOME
  [ ! -d ~/.gnus.d ] && mkdir ~/.gnus.d
  [ -f ~/.gnus.d/.gnus.el ] && rm .gnus.d/.gnus.el
  ln -s ../.emacs.d/gnus.el .gnus.d/.gnus.el )

