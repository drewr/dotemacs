#!/bin/sh

EMACS=$HOME/.emacs.d
GNUS=$HOME/Dropbox/.gnus.d

( cd d; tar cf - . ) \
| ( mkdir -p $EMACS; \
    cd $EMACS; \
    tar xvf - )

( mkdir -p $GNUS
  ln -snf ../../.emacs.d/gnus.el $GNUS/.gnus.el )
