#!/bin/sh

EMACS=$HOME/.emacs.d
GNUS=$HOME/Dropbox/.gnus.d

( cd d; tar cf - . ) \
| ( mkdir -p $EMACS; \
    cd $EMACS; \
    tar xvf - )

ln -snf $GNUS ~/.gnus.d

emacs --batch \
  --eval "(byte-recompile-directory (expand-file-name \"${EMACS}\") 0)"
